Drop each lecture's slide PDF in this folder named `classNN.pdf` (e.g. `class01.pdf` for Class 1, `class02.pdf` for Class 2, ...). Each class's topic name in the Course Schedule table on [/stor155fall2026/](/stor155fall2026/) is already hyperlinked to this path, so the slides go live automatically once uploaded. No need to edit the page.
